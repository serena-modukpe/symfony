{% extends 'base.html.twig' %}

{% block title %}RolesHabilitations{% endblock %}

{% block body %}
    <h1>RolesHabilitations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roles_habilitation.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ roles_habilitation.role.libelle }}</td>
            </tr>
            <tr>
                <th>Habilitation</th>
                <td>{{ roles_habilitation.habilitation.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roles_habilitations_index') }}">back to list</a>

    <a href="{{ path('app_roles_habilitations_edit', {'id': roles_habilitation.id}) }}">edit</a>

    {{ include('roles_habilitations/_delete_form.html.twig') }}
{% endblock %}
