{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.user.nom }}</td>
            </tr>
            <tr>
                <th>Rôle</th>
                <td>{{ utilisateur.role.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateurs_index') }}"class="btn btn-info text-white">Retour à la liste</a>

    <a href="{{ path('app_utilisateurs_edit', {'id': utilisateur.id}) }}"class="btn btn-dark text-white">Modifier</a>

    {{ include('utilisateurs/_delete_form.html.twig') }}
{% endblock %}
